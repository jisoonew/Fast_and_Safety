<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

 
<mapper namespace="memberMapper">

    <insert id="insert_today_delivery">
        insert into today_delivery (u_id, td_name, td_phone, td_volume, td_address) values
                          (#{u_id}, #{td_name}, #{td_phone}, #{td_volume}, #{td_address})
    </insert>

	<select id="list_today_delivery" resultType="com.fs.vo.Today_delivery_VO">
	 	select u_id, td_name, td_phone, td_volume, td_address from today_delivery
	</select>

</mapper>